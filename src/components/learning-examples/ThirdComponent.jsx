import { Component } from "react";

export default class ThirdComponent extends Component{ // class won't have the () or paranthesis
    render(){ // square brackets : 
      return(
        <div className = "ThirdComp"> This is the third component : </div>
      )
    }
}